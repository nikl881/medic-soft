{% extends 'base.html.twig' %}

{% block title %}MedicSoft | Profile {% endblock %}

{% block main %}
    <div class="container">
        <h1>Profile settings</h1>
        <div class="row">
            <div class="col-4">
                <div class="card">
                    <div class="card-header">
                        <i class="fas fa-user"></i> Profile information
                    </div>
                    <div class="card-body">
                        <div class="d-flex">
                            <div class="fixed-width-100">Name:</div>
                            <div>{{ app.user.name }}</div>
                        </div>
                        <div class="d-flex">
                            <div class="fixed-width-100">Last name:</div>
                            <div>{{ app.user.lastName }}</div>
                        </div>
                        <div class="d-flex">
                            <div class="fixed-width-100">Title:</div>
                            <div>{{ app.user.title }}</div>
                        </div>
                        <div class="d-flex">
                            <div class="fixed-width-100">Specialty:</div>
                            <div>{{ app.user.medicalSpecialty }}</div>
                        </div>
                        <div class="d-flex">
                            <div class="fixed-width-100">Licence:</div>
                            <div>{{ app.user.licenceNumber }}</div>
                        </div>
                        <div class="d-flex">
                            <div class="fixed-width-100">Start date:</div>
                            <div>{{ app.user.dateCreated|date("d/m/Y") }}</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4">
                <div class="card">
                    <div class="card-header">
                        <i class="fas fa-edit"></i> Update profile information
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}
                        {{ form_row(form.name) }}
                        {{ form_row(form.lastName) }}
                        {{ form_row(form.title) }}
                        {{ form_row(form.medicalSpecialty) }}
                    </div>
                </div>
            </div>
            <div class="col-4">
                <div class="card">
                    <div class="card-header">
                        <i class="fas fa-edit"></i> Update profile image
                    </div>
                    <div class="card-body">
                        <div class="mb-2">
                        <img src="{{ asset('build/images/medicsoft-menu.png') }}" height="50" alt="profile avatar">
                        </div>
                        {{ form_row(form.profileImage) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
